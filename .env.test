# Frontend (Vite) - VITE_ prefix required for client exposure
VITE_SUPABASE_URL=https://demo-project.supabase.co
VITE_SUPABASE_ANON_KEY=anon-demo-key-xxxxxxxxxxxxxxxxxxxxxxxx
VITE_API_BASE=http://localhost:3000

# Backend (test values)
DATABASE_URL=postgres://testuser:testpass@localhost:5432/psygdb
SUPABASE_SERVICE_KEY=service-role-demo-key-xxxxxxxxxxxxxxxx
JWT_SECRET=test_jwt_secret_please_replace
PORT=3000
NODE_ENV=development

# Optional SMTP (for local email testing via mailtrap, etc.)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=mailtrap_user
SMTP_PASS=mailtrap_pass
